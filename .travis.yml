language: r
cache: packages

before_install:
  - Rscript -e 'install.packages(pkgs = c("cleanNLP", "covr"))'
  - Rscript -e 'cleanNLP::cnlp_download_corenlp(type = "en")'

warnings_are_errors: false

notifications:
  email:
  on_success: change
  on_failure: change

after_success:
  - Rscript -e 'library(covr);codecov(token = "d02e1347-6fa5-4be1-97d5-92a7326ee097")'
